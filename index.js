import React from 'react';
import ReactDOM from 'react-dom/client'; // Import yang diperbarui
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root')); // Membuat root
root.render(<App />); // Render aplikasi